import * as React from 'react';
import * as ReactDOM from 'react-dom';

export const EFS: React.FC = () => (
    <div>
        EFS
    </div>
);

ReactDOM.render(<EFS />, document.getElementById('root'));